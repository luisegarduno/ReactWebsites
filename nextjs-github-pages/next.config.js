// @ts-check

/**
 * @type {import('next').NextConfig}
 **/
const nextConfig = {
  output: 'export',
  basePath: '/ReactWebsites',
  experimental: {
    appDir: true,
  },
}

module.exports = nextConfig
